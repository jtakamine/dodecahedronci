dodecregistry:
        build: ./dodecregistry
        ports:
                - "8000:8000"

dodecbuild:
        build: ./dodecbuild
        ports:
                - "8001:8000"
        environment:
                DODEC_HOME:
                DODEC_GITHUB_USER:
                DODEC_GITHUB_PASSWORD:
                DODEC_DOCKER_USER:
                DODEC_DOCKER_PASSWORD:
                DODEC_DOCKER_EMAIL:
        privileged: true
        links:
                - dodecregistry

dodecdeploy:
        build: ./dodecdeploy
        ports:
                - "8002:8000"
        links:
                - dodecregistry
